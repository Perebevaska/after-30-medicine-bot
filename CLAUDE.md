# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## About
Telegram бот для напоминаний о приёме лекарств с поддержкой гибкого расписания, статистики, экспорта в PDF и определения часовых поясов. Параллельно разрабатывается FastAPI-бэкенд для Telegram Mini App.

## Stack
- **Language**: Python 3.14
- **Framework**: python-telegram-bot 22.7 (async API)
- **Scheduler**: APScheduler 3.11.2 (задача каждую минуту, **только в bot.py**)
- **Database**: PostgreSQL 18 через psycopg 3.3 + psycopg_pool (пул соединений)
- **API**: FastAPI 0.136 + Uvicorn 0.48 (`api/`)
- **Timezone**: pytz, timezonefinder, geopy (Nominatim кэшируется как `_geolocator` на уровне модуля)
- **PDF**: fpdf2 + DejaVuSans TTF (`/usr/share/fonts/truetype/dejavu/`)
- **Container**: Podman, multi-stage Containerfile (python:3.14-slim + fonts-dejavu-core)

## Architecture

### Структура
```
med-bot/
├── bot.py                  # точка входа — только main() + регистрация handlers
├── database.py             # PostgreSQL CRUD через get_connection() (psycopg pool)
├── scheduler.py            # send_reminders() каждую минуту
├── schedule_utils.py       # чистая логика «положенных приёмов»
├── streak.py               # F2: серия идеальных дней
├── constants.py            # States, MEAL_LABELS, MONTHS_GEN, MAX_MEDICATIONS_PER_USER
├── utils.py                # handle_db_errors, get_tz_for_user, cancel, escape_md, parse_time
├── broadcast.py            # standalone скрипт рассылки
├── migrate_sqlite_to_pg.py # P4: миграция данных SQLite → PostgreSQL
├── Containerfile           # multi-stage Podman/Docker образ
├── compose.yaml            # podman compose (local: postgres + bot)
├── deploy/
│   ├── quadlet/            # Quadlet-юниты для VPS (pod, db, bot, volume)
│   └── backup/             # pg_dump backup/restore + systemd timer
├── api/
│   ├── main.py             # FastAPI app (lifespan: init_pool/close_pool)
│   ├── auth.py             # Telegram initData HMAC-SHA256 валидация
│   └── routers/
│       ├── medications.py  # CRUD + пауза
│       ├── today.py        # «на сегодня» + log intake
│       ├── stats.py        # week / adherence / streak
│       ├── stock.py        # учёт запаса
│       ├── dependents.py   # caregiver подопечные
│       ├── settings.py     # настройки пользователя
│       └── export.py       # PDF через asyncio.to_thread
└── handlers/
    ├── meds.py             # add/edit/delete medications
    ├── stats.py            # stats_week, show_week_plan, adherence
    ├── export.py           # PDF экспорт + standalone builders для API
    ├── settings.py         # settings, about, reminder_mode toggle, daily plan
    ├── admin.py            # админ-панель (только ADMIN_ID)
    ├── caregiver.py        # caregiver-режим: подопечные
    ├── stock.py            # F5: экран «📦 Запас»
    └── timezone.py         # start, timezone setup, main menu, Лекарства на сегодня
```

### Схема БД (PostgreSQL)
5 активных таблиц, все PK — `INTEGER GENERATED BY DEFAULT AS IDENTITY`:
- `users` (telegram_id BIGINT UNIQUE, username, timezone, reminder_mode, time_morning/lunch/evening/night, daily_plan_enabled, daily_plan_time, **caregiver_enabled**, created_at TEXT)
- `dependents` (user_id FK, name)
- `medications` (user_id FK, name, dosage, meal_relation, times_per_day, active, **dependent_id** FK NULL, **stock_qty** REAL NULL, **units_per_dose** REAL, **low_stock_days** INTEGER, **paused** INTEGER 0/1, created_at TEXT)
- `schedule_rules` (medication_id FK, reminder_time, frequency, interval_days, weekdays, month_day, anchor_date, dosage NULL=наследует из medications)
- `intake_log` (medication_id FK, scheduled_time, status, taken_at TEXT UTC)

**Соединение БД** (`get_connection`): psycopg_pool ConnectionPool, `row_factory=dict_row`, `init_pool(dsn)` вызывается из `bot.py:main()` и `api/main.py:lifespan`. Все таймстампы — TEXT `'YYYY-MM-DD HH:MM:SS'` UTC.
**Индексы**: `medications(user_id, active)`, `medications(dependent_id)`, `schedule_rules(medication_id)`, `intake_log(medication_id, scheduled_time)`, `intake_log(taken_at)`.

### schedule_rules — типы frequency
| frequency | поля | описание |
|-----------|------|----------|
| `daily` | — | каждый день |
| `interval` | `interval_days`, `anchor_date` | каждые N дней от anchor_date |
| `weekdays` | `weekdays` | по дням недели, '1,3,5' (пн=1, вс=7) |
| `monthly` | `month_day` | раз в месяц, N-го числа |

### API эндпоинты (FastAPI)
- `GET /health` — статус
- `GET|POST|PUT|DELETE /medications` — CRUD лекарств
- `POST /medications/{id}/pause|resume` — пауза
- `GET /today`, `POST /today/intake` — расписание на сегодня + отметка приёма
- `GET /stats/week|adherence|streak` — статистика
- `GET|PUT|POST|DELETE /medications/{id}/stock*` — учёт запаса
- `GET|POST|DELETE /dependents` — подопечные
- `GET|PUT /settings/*` — настройки пользователя
- `GET /export/plan|week|adherence|doctor` — PDF

Авторизация: `Authorization: tma <url-encoded initData>` (Telegram HMAC-SHA256, срок 24ч).
Синхронные DB-вызовы оборачиваются в `asyncio.to_thread()`.
**APScheduler запускается только в bot.py** — в API не стартуем.

### PDF builders (handlers/export.py)
Добавлены standalone функции без привязки к Telegram:
- `build_plan_pdf(telegram_id)` → `BytesIO | None`
- `build_week_stats_pdf(telegram_id)` → `BytesIO | None`
- `build_adherence_pdf(telegram_id)` → `BytesIO | None`
- `build_doctor_pdf(telegram_id, user_label)` → `BytesIO | None`

## Commands

```bash
# Бот
source venv/bin/activate
python3 bot.py

# API (из корня проекта)
uvicorn api.main:app --reload --port 8000

# Установка зависимостей (бот)
pip install -r requirements.txt

# Установка зависимостей (API)
pip install -r requirements-api.txt

# Миграция данных SQLite → PostgreSQL
python3 migrate_sqlite_to_pg.py           # идемпотентно
python3 migrate_sqlite_to_pg.py --clean   # с очисткой целевой БД

# Podman compose (локально: postgres + bot)
PATH="$PATH:~/.local/bin" podman compose up -d
PATH="$PATH:~/.local/bin" podman compose down

# Бэкап PostgreSQL
export $(grep -v '^#' .env | xargs) && bash deploy/backup/backup.sh
bash deploy/backup/restore.sh /path/to/dump.dump

# Тесты
python -m pytest -q
```

## Тесты
Всего **217** тестов (pytest), все на PostgreSQL (`medbot_test`).

**Конфиг**: `pytest.ini` (`testpaths = tests`); `tests/conftest.py` — сессионный пул + per-test TRUNCATE + autouse `_clear_rate_counters`.
**БД**: `conftest.py` инициализирует `medbot_test` через `init_pool(TEST_DSN)` + `TRUNCATE ... RESTART IDENTITY CASCADE` перед каждым тестом.
**API**: фикстура `api_client` — `TestClient` с `dependency_overrides[require_telegram_user]`.

Тестовые файлы:
- `test_pure.py` — чистые функции (parse_time, escape_md, rule_fires_today, ...)
- `test_handlers.py` — save-хендлеры add/edit (DB мокается в namespace handlers.meds)
- `test_menu.py` — навигация меню, `_today_keyboard`
- `test_conv_structure.py` — снапшот структуры ConversationHandler
- `test_schedule_utils.py` — положенные приёмы, days_of_stock_left
- `test_adherence_db.py` — get_adherence_rules, get_taken_counts
- `test_adherence_handler.py` — show_adherence (текст %, _pct_color)
- `test_adherence_export.py` — PDF adherence (сигнатура %PDF)
- `test_doctor_report.py` — PDF отчёт врача
- `test_pause.py` — DB-фильтры паузы + toggle-хендлер
- `test_streak.py` — compute_streak, streaks_by_subject
- `test_stock_db.py` — DB-слой запаса F5
- `test_stock_intake.py` — списание запаса через handle_intake_callback
- `test_delete_user_data.py` — полное удаление данных пользователя
- `test_preset_migration.py` — миграция правил при смене пресета
- `test_api_auth.py` — A2: валидный/просроченный/поддельный initData
- `test_api_endpoints.py` — A3: все эндпоинты API (22 теста)
- `test_api_a5.py` — A5: изоляция пользователей, CORS, rate limit, edge cases (14 тестов)
- `test_security_blockers.py` — аудит: S1/S2 (IDOR), S3 (username), B5 (валидация правил), 9 тестов

**Перед рефакторингом хендлеров**: `python -m pytest -q` до и после.

## Conversational States
(в `constants.py`) `NAME, DOSAGE, MEAL, TIMES` (0-3), `EDIT_NAME..EDIT_TIMES` (5-8), `SETUP_TZ, SETUP_CITY` (10-11), `FREQ_TYPE..FREQ_MONTHDAY` (12-15), `EDIT_FREQ_TYPE..EDIT_FREQ_MONTHDAY` (17-20), `PRESET_TIME` (22), `DAILY_PLAN_TIME` (23), `DOSAGE_B..FREQ_MONTHDAY_B` (29-34), `EDIT_DOSAGE_B` (35), `SELECT_DEPENDENT` (36), `ADD_DEPENDENT_NAME` (37), `STOCK_INPUT` (38).

## Error Handling
- `DatabaseError` — custom exception в `database.py`
- Декоратор `@handle_db_errors` из `utils.py` — оборачивает Telegram handler-функции
- Ошибки БД → **stderr** (C5: убран `db_errors.log`, используется `StreamHandler(sys.stderr)`)
- API ошибки: 401 без валидного initData, 404 если ресурс не найден, 400 при нарушении лимитов или невалидном timezone, 422 при ошибках валидации (detail всегда строка)
- PDF генерируется в `asyncio.to_thread` чтобы не блокировать event loop

## Configuration
`.env` файл (не коммитится, шаблон в `.env.example`):
```
BOT_TOKEN=токен_от_BotFather
ADMIN_ID=telegram_id_админа
DATABASE_URL=postgresql://medbot:ПАРОЛЬ@localhost/medbot
MINIAPP_ORIGIN=https://your-miniapp-domain.com   # CORS; "*" = dev-режим (warning в логах)
RATE_LIMIT_PER_MINUTE=60                          # rate limiter (sliding window per IP)
TRUST_PROXY=false                                 # true → брать IP из X-Forwarded-For (за Caddy)
```

Тесты используют `DATABASE_URL=postgresql://medbot:medbot@127.0.0.1/medbot_test` (задаётся в `tests/conftest.py`).

## Key Behaviors
- `init_pool()` вызывается в `bot.py:main()` до `init_db()` и в `api/main.py:lifespan`; пул идемпотентен (no-op если уже создан)
- Часовой пояс запрашивается при `/start` если не задан
- Напоминания в local time пользователя
- Режим напоминаний: `once` или `repeat` (каждые 5 минут до подтверждения, до 2 часов)
- Лимит лекарств: `MAX_MEDICATIONS_PER_USER = 10`
- `log_intake` — upsert по (medication_id, scheduled_time, дата в TZ пользователя)
- **Смена пресета мигрирует правила**: `set_user_time_preset()` переносит все `schedule_rules` пользователя с reminder_time == старое значение на новое
- **Один проход планировщика**: `get_active_schedule_rows()` — один JOIN-запрос на весь run
- **Разная дозировка**: правила А `dosage=NULL`, Б `dosage=dosage_b`; планировщик: `rule_dosage or med_dosage`
- `handle_intake_callback` парсит `status:med_id:HH:MM`
- **APScheduler только в bot.py** — api/main.py не запускает scheduler

## Known Issues

### 🔲 К исправлению (аудит 2026-06-02)

Полный список и план — в разделе **[Аудит и план исправлений](#аудит-и-план-исправлений-2026-06-02)** ниже.

| # | Файл | Проблема | Приоритет |
|---|------|----------|-----------|
| ~~S1~~ | `scheduler.py:180` | ✅ Исправлено: проверка владельца в `handle_intake_callback` + тест | 🔴 |
| ~~S2~~ | `api/routers/medications.py:54` | ✅ Исправлено: валидация `dependent_id` по владельцу + тест | 🔴 |
| ~~B1~~ | `broadcast.py:18` | ✅ Исправлено: рассылка читает PostgreSQL через `get_connection()` | 🔴 |
| ~~S3~~ | `database.py:188` | ✅ Исправлено: `COALESCE(EXCLUDED.username, users.username)` + тест | 🟡 |
| ~~B2~~ | `database.py` | ✅ Исправлено: `datetime.now(timezone.utc)` везде | 🟡 |
| ~~B3~~ | `scheduler.py` | ✅ Исправлено: DB-вызовы планировщика и callback через `asyncio.to_thread` | 🟡 |
| ~~B4~~ | `schedule_utils.py:30` | ✅ Исправлено: guard на `interval_days` NULL/0 + тест | 🟡 |
| ~~S4~~ | `api/main.py:28` | ✅ Исправлено: очистка ключей + `TRUST_PROXY` (X-Forwarded-For) | 🟡 |
| ~~S5~~ | `api/main.py:56` | ✅ Исправлено: warning при открытом CORS (`*`) | 🟡 |
| ~~B5~~ | `api/routers/medications.py:15` | ✅ Исправлено: Pydantic-валидаторы правил + 4 теста | 🟡 |
| O1 | репо | Артефакты `med_bot.db` (персданные!), `db_errors.log`, `bot_run.log` в рабочей копии — удалить вручную | 🟢 |
| O2 | `.claude/CLAUDE.md` | Дубль CLAUDE.md описывает устаревшую SQLite-версию | 🟢 |
| O3 | `handlers/meds.py` | Монолит 108 КБ — разбить перед M3 | 🟢 |
| O4 | `api/routers/*` | Повтор `get_or_create_user` в ~12 эндпоинтах | 🟢 |
| O5 | боты/PDF | Markdown v1 хрупкий — рассмотреть MarkdownV2/HTML | 🟢 |
| O6 | зависимости | `pip-audit` + пины версий (привязать к D3) | 🟢 |

---

## Аудит и план исправлений (2026-06-02)

Full-stack code review + фокус на безопасности БД и кибербезопасности.
**Хорошие практики уже в проекте:** все SQL параметризованы (`%s`/`ANY`), нет f-string-SQL с пользовательскими данными; `.env`/`med_bot.db`/`*.log` не в git; initData валидируется constant-time HMAC (`hmac.compare_digest`) + срок 24ч; `telegram_id` берётся только из подписи, не от клиента; владелец проверяется через `get_medication_by_id(id, user_id)` в большинстве API-эндпоинтов; контейнер от непривилегированного `appuser`.

**A4 и A5 — подтверждены выполненными** (rate limiter, CORS, нормализация 422, Pydantic-Literal, pytz-валидация TZ, 14 тестов изоляции присутствуют). Roadmap отмечен корректно.

### 🔴 Блокеры (делать первыми)

**S1 · IDOR в боте — `handle_intake_callback`** (`scheduler.py:180-208`)
callback_data `status:med_id:HH:MM` контролируется клиентом; `log_intake(medication_id, …)` и `apply_intake_stock(medication_id, …)` вызываются без проверки, что `med_id` принадлежит нажавшему. Модифицированный клиент пишет/перезаписывает чужой `intake_log` и меняет чужой `stock_qty`.
→ Перед записью: `uid = get_or_create_user(effective_user.id); if not get_medication_by_id(med_id, uid): return`. `[тест]` на чужой med_id.

**S2 · IDOR в API — `create_medication.dependent_id`** (`api/routers/medications.py:54-69`, `database.py:add_medication`)
`dependent_id` из тела не проверяется на принадлежность user_id → лекарство можно привязать к чужому подопечному.
→ Валидировать `dependent_id ∈ get_dependents(user_id)` перед `add_medication` (иначе 404/400). `[тест]`.

**B1 · `broadcast.py` читает мёртвый SQLite** (`broadcast.py:7,18,24`)
После миграции P1–P4 рассылка идёт по старым users из `med_bot.db`; пользователи в PostgreSQL её не получат.
→ Переписать `get_all_user_ids()` на `database.get_connection()` (psycopg).

### 🟡 Важно

**S3 · Затирание username** (`database.py:188`) — `ON CONFLICT DO UPDATE SET username = EXCLUDED.username`; API всегда зовёт без username → NULL на каждый запрос.
→ `SET username = COALESCE(EXCLUDED.username, users.username)`.

**B2 · `datetime.utcnow()` устарел** (`database.py`: `get_history`, `get_history_by_days`, `get_admin_stats`, `log_intake`) — DeprecationWarning в 3.14, naive datetime.
→ `datetime.now(timezone.utc).strftime(...)`.

**B3 · Блокирующий I/O в event loop** (`scheduler.py:44`, `handle_intake_callback`) — синхронный psycopg в корутине каждую минуту блокирует loop.
→ `await asyncio.to_thread(get_active_schedule_rows)` и аналогично для `log_intake`/`apply_intake_stock`/`get_schedules_by_medication`.

**B4 · `interval`-правило без `interval_days`** (`schedule_utils.py:30`) — `% None`/`% 0` → TypeError/ZeroDivisionError; в аналитике (API) не поймано.
→ `if not row["interval_days"]: return False` в начале ветки.

**S4 · Rate limiter** (`api/main.py:28-39`) — `_counters` растёт по уникальным IP (пустые ключи не чистятся); per-process (не работает с >1 воркера); за прокси (Caddy, D1) видит IP прокси.
→ Чистить пустые ключи; читать `X-Forwarded-For` от доверенного прокси; для прода — общий стор (Redis) или задокументировать single-worker. Привязать к D1/D2.

**S5 · CORS fail-open** (`api/main.py:56`) — дефолт `*`.
→ Вне dev требовать явный `MINIAPP_ORIGIN`; при отсутствии — не поднимать или логировать warning.

**B5 · Нет серверной валидации правил** (`api/routers/medications.py:15-23`) — `reminder_time`, `weekdays`, `month_day`, `interval_days`, `anchor_date` принимаются без проверки.
→ Pydantic-валидаторы: `reminder_time` ⇒ `HH:MM` (через `parse_time`), `month_day` 1–31, `interval_days` > 0 + `anchor_date` обязателен для `interval`, формат `weekdays`. Закрывает и B4 на входе. `[тест]`.

### 🟢 Желательно (гигиена / рефакторинг под roadmap)

**O1 · Артефакты в рабочей копии** — удалить `med_bot.db` (содержит персданные!), `db_errors.log`, `bot_run.log`. Уже в `.gitignore`, но лежат на диске.
**O2 · Дубль `.claude/CLAUDE.md`** — описывает устаревшую SQLite-архитектуру; удалить или синхронизировать.
**O3 · `handlers/meds.py` (108 КБ, монолит)** — разбить (add/edit/common) и вынести переиспользуемую валидацию расписания **перед M3** (форма-редактор в Mini App).
**O4 · Повтор `get_or_create_user`** в ~12 эндпоинтах — лишний INSERT+roundtrip на запрос; резолвить user_id в зависимости (один раз на запрос).
**O5 · Markdown v1 хрупкий** — `parse_mode="Markdown"` + неполный `escape_md`; рассмотреть MarkdownV2/HTML для пользовательских строк (имена лекарств/подопечных).
**O6 · Пины зависимостей** — прогнать `pip-audit`; зафиксировать версии для воспроизводимых сборок (привязать к D3).

### Статус
✅ **Все 🔴 и 🟡 закрыты** (S1–S5, B1–B5) — 217 тестов зелёные.
🔲 Остались 🟢-гигиена: **O1** (удалить артефакты `med_bot.db`/`*.log` вручную),
**O2** (дубль `.claude/CLAUDE.md`), **O3** (разбить `meds.py` перед M3),
**O4** (повтор `get_or_create_user`), **O5** (Markdown v1), **O6** (`pip-audit`).

> Замечание по S4: rate limiter всё ещё per-process (in-memory). Для нескольких
> воркеров/реплик в проде нужен общий стор (Redis) — привязать к D2/D3.

---

## Roadmap

**Размер:** `S` < 1д · `M` 2–5д · `L` 1–2 нед  
**Критичность:** 🔴 блокер · 🟡 важно · 🟢 желательно  
**Тест:** `[тест]` — обязательная проверка перед тем как считать задачу закрытой

**Критичный путь:** ~~P0→P1→P2→P3→P4→P5~~→~~C1→C2→C3→C4→C5~~→~~A1→A2→A3→A4→A5~~→M1→M2→D1

---

### Фаза 1 — Postgres ✅

**P0** ✅ Таймстампы → TEXT, булевы → INTEGER(0/1)  
**P1** ✅ sqlite3 → psycopg3 + ConnectionPool, DATABASE_URL  
**P2** ✅ DDL: GENERATED BY DEFAULT AS IDENTITY, убраны PRAGMA  
**P3** ✅ ? → %s, RETURNING id, datetime в Python, ANY(%s)  
**P4** ✅ `migrate_sqlite_to_pg.py` (--clean, счётчики, sequence reset)  
**P5** ✅ DB-тесты на PostgreSQL (conftest.py + TRUNCATE RESTART IDENTITY CASCADE)

---

### Фаза 2 — Podman + VPS ✅

**C1** ✅ Containerfile (multi-stage, python:3.14-slim, fonts-dejavu-core, appuser)  
**C2** ✅ Секреты через --env-file, .env.example, .containerignore  
**C3** ✅ compose.yaml (local) + Quadlet-юниты (pod/db/bot/pgdata)  
**C4** ✅ backup.sh + restore.sh + systemd timer; restore протестирован на medbot_test  
**C5** ✅ db_errors → stderr (StreamHandler)

---

### Фаза 3 — FastAPI (Backend API)

**A1** ✅ FastAPI skeleton (lifespan pool ownership, /health)  
**A2** ✅ Auth: HMAC-SHA256 initData, `Authorization: tma <data>`, 3 теста  
**A3** ✅ Эндпоинты: medications/today/stats/stock/dependents/settings/export; 22 теста  

**A4** ✅ Инфра API
CORSMiddleware (MINIAPP_ORIGIN env), sliding-window rate limiter (60 rpm per IP, без внешних зависимостей), RequestValidationError → строка, Pydantic Literal для meal_relation/frequency, /today/intake 404 guard, /settings/timezone pytz-валидация.

**A5** ✅ Тесты API (14 тестов)
Изоляция: list/delete/update/stock/pause чужого → 404. CORS headers. Rate limit 429. Unified error format. Edge cases: invalid meal_relation, unknown med intake, invalid timezone, days_left.

---

### Фаза 4 — Mini App (Frontend)

M1 стартует после A2 (против мок-API). M2–M7 параллельно после M1+A3.

**M1** `S` 🔴 Стек: Vite + React/Svelte, `@telegram-apps/sdk`, TanStack Query  
**M2** `M` 🔴 Дашборд: сегодня + серия + adherence + ✅/❌  
**M3** `L` 🔴 Редактор лекарства `[тест]` (форма вместо ConversationHandler)  
**M4** `M` 🟡 Список лекарств (карточки, пауза, удаление)  
**M5** `M` 🟡 Запас (остаток/пополнение/порог/прогноз)  
**M6** `M` 🟡 Статистика (adherence chart, серия, PDF)  
**M7** `M` 🟢 Настройки (пресеты, режим, caregiver)  
**M8** `S` 🟡 Регистрация в BotFather + кнопка «📱 Приложение» в боте

---

### Фаза 5 — Деплой и эксплуатация

**D1** `S` 🔴 Caddy (HTTPS, `/api` → api, `/` → miniapp) `[тест]`  
**D2** `M` 🟡 Quadlet для всех 5 контейнеров (db, bot, api, miniapp, caddy)  
**D3** `M` 🟢 CI/CD (ghcr + Podman auto-update или Coolify)  
**D4** `M` 🟢 Наблюдаемость (логи, healthcheck, метрики напоминаний)

---

### UX-интеграция бот ↔ Mini App

**U1** `S` 🟡 Deep links из напоминаний в Mini App  
**U2** `M` 🟢 Предлагать Mini App при add/edit (ConversationHandler как fallback)  
**U3** `S` 🟢 «📊 Подробнее →» deep link в Mini App под текстовой статистикой

---

### Продуктовые фичи

**F7** `L` 🟡 Caregiver-расширение: уведомления опекуну, связь двух реальных пользователей
