[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
# Сторонняя deprecation (starlette.testclient + httpx) — не наш код, глушим адресно.
filterwarnings =
    ignore:Using `httpx` with `starlette.testclient` is deprecated:
